下面是一份可以直接交给 **Trae** 继续实现的《musiclua 项目计划书》。  
你已经按完整版目录建好文件了，所以这份计划书会按你现有结构来写。

---

# musiclua 项目计划书

## 1. 项目名称

```text
musiclua
```

完整定位：

```text
一个用 Lua 编写的终端音乐播放器包，支持本地音乐扫描、播放列表管理、TUI 交互播放，并预留合法 RSS/Podcast/音频 URL 导入能力。
```

---

## 2. 项目目标

### 2.1 核心目标

实现一个 Lua 包，提供以下能力：

1. 扫描本地目录中的音频文件。
2. 将音频文件组织成播放列表。
3. 使用 TUI 界面进行交互。
4. 使用 mpv 作为音频播放后端。
5. 支持基本播放控制：
   - 播放
   - 暂停
   - 下一首
   - 上一首
   - 音量调节
   - 搜索过滤
   - 退出
6. 提供可扩展的 source/player/tui 架构。
7. 后续支持合法网络音频源，例如 RSS/Podcast。

---

### 2.2 第一阶段目标，也就是 MVP

第一阶段只做本地音乐播放。

目标命令：

```bash
musiclua ~/Music
```

执行后进入 TUI：

```text
 musiclua                                    Local Library

  1  song_a.ogg
  2  song_b.mp3
> 3  song_c.flac
  4  podcast_episode_01.mp3
  5  demo_track.wav

 Now Playing: song_c.flac
 Status: playing | 01:24 / 03:52 | volume: 80%

 [enter] play  [space] pause  [n] next  [p] prev  [/] search  [q] quit
```

MVP 必须支持：

1. 扫描指定目录下的音频文件。
2. 显示文件列表。
3. 使用 `j/k` 或方向键选择歌曲。
4. 使用 `Enter` 播放当前歌曲。
5. 使用 `space` 暂停/继续。
6. 使用 `n/p` 切换下一首/上一首。
7. 使用 `/` 搜索过滤歌曲。
8. 使用 `q` 退出。
9. 退出时正确关闭 mpv。

---

## 3. 非目标

为了避免项目膨胀，以下内容不列入第一阶段：

1. 不做 GUI。
2. 不做图形界面。
3. 不做音乐标签编辑器。
4. 不做歌词显示。
5. 不做在线音乐平台账号登录。
6. 不做版权内容抓取。
7. 不做绕过 DRM、登录、付费墙、验证码、反爬机制的功能。
8. 不做复杂的音频转码。
9. 不做数据库索引。
10. 不做移动端适配。

---

## 4. 合规要求

`musiclua` 只允许处理合法音频来源。

允许的 source：

1. 本地用户自有音频文件。
2. 用户主动提供的合法音频 URL。
3. 公开允许的 RSS/Podcast feed。
4. 用户本地 `.m3u` / `.m3u8` 播放列表。
5. 明确允许下载或播放的公开音频资源。

禁止实现：

1. 抓取受版权保护且未授权的音乐网站。
2. 绕过登录墙。
3. 绕过付费墙。
4. 绕过 DRM。
5. 绕过验证码。
6. 绕过 IP 限制。
7. 伪造客户端身份进行非法抓取。
8. 批量下载未经授权的音乐资源。

如果后续实现 `sources/rss.lua` 或 `sources/url.lua`，必须保持这个原则。

---

## 5. 用户场景

### 场景 1：播放本地音乐

用户执行：

```bash
musiclua ~/Music
```

musiclua 扫描目录：

```text
~/Music
```

找到：

```text
mp3
ogg
wav
flac
m4a
opus
```

等音频文件，然后进入 TUI。

用户选择一首歌，按 `Enter` 播放。

---

### 场景 2：搜索本地音乐

用户按：

```text
/
```

输入：

```text
live
```

列表过滤为包含 `live` 的歌曲。

按 `Esc` 清除搜索。

---

### 场景 3：暂停和切歌

播放中：

```text
space 暂停/继续
n 下一首
p 上一首
+ 音量增加
- 音量减少
```

---

### 场景 4：未来添加 RSS/Podcast

后续版本支持：

```bash
musiclua add-rss https://example.com/podcast.rss
```

musiclua 解析 RSS 中的 enclosure 音频地址，并加入播放列表。

该功能只用于合法 RSS/Podcast。

---

## 6. 总体架构

```text
┌────────────────────────────────────────┐
│                                     bin/musiclua
└───────────────────────┬────────────────┘
                        │
                        ▼
┌────────────────────────────────────────┐
│                                 musiclua.app
└──────┬──────────────┬──────────────┬────┘
       │              │              │
       ▼              ▼              ▼
┌────────────┐ ┌────────────┐ ┌────────────┐
│ library    │ │ playlist   │ │ player     │
└─────┬──────┘ └────────────┘ └─────┬──────┘
      │                              │
      ▼                              ▼
┌────────────┐               ┌────────────┐
│ sources    │               │ mpv IPC    │
└────────────┘               └────────────┘

       TUI 层负责展示和交互：
┌────────────────────────────────────────┐
│ tui.app + tui.list + tui.statusbar     │
└────────────────────────────────────────┘
```

模块关系原则：

1. TUI 不直接扫描文件。
2. Player 不直接读取目录。
3. Library 只负责生成 track 列表。
4. Playlist 只负责管理 track 顺序和当前索引。
5. Player 只负责播放当前 track。
6. App 负责连接所有模块。

---

## 7. 技术选型

### 7.1 语言

推荐：

```text
Lua 5.4
```

也可以兼容：

```text
Lua 5.1 / LuaJIT
```

但第一版建议先以 Lua 5.4 为主。

---

### 7.2 TUI 方案

主方案：

```text
lua-curses
```

备选方案：

```text
ANSI escape codes
```

第一阶段建议：

```text
优先使用 curses。
如果 curses 安装困难，可先做 ANSI 简版。
```

TUI 只运行在终端，不做 GUI。

---

### 7.3 播放后端

主方案：

```text
mpv
```

原因：

1. 音频格式支持广。
2. 支持暂停、进度、音量、播放列表。
3. 支持 JSON IPC。
4. 适合无 GUI 的终端播放器。
5. 跨平台相对成熟。

启动方式建议：

```bash
mpv --no-video --idle=yes --input-ipc-server=<socket_path>
```

Linux/macOS 示例：

```bash
mpv --no-video --idle=yes --input-ipc-server=/tmp/musiclua.sock
```

---

### 7.4 Lua 依赖

建议依赖：

```text
luafilesystem
luasocket
luasec
dkjson
lua-curses
```

用途：

| 依赖 | 用途 |
|---|---|
| luafilesystem | 目录扫描、文件属性 |
| luasocket | mpv IPC 通信，后续 HTTP |
| luasec | 后续 HTTPS/RSS |
| dkjson | JSON 编解码 |
| lua-curses | TUI |

如果 `lua-curses` 不方便安装，则降级为 ANSI TUI。

---

## 8. 项目目录结构

你已经按完整版建立文件。以下结构作为正式说明。

```text
musiclua/
├── README.md
├── LICENSE
├── .gitignore
├── config.example.lua
│
├── bin/
│   └── musiclua
│
├── musiclua/
│   ├── init.lua
│   ├── version.lua
│   ├── app.lua
│   ├── config.lua
│   ├── library.lua
│   ├── track.lua
│   ├── playlist.lua
│   │
│   ├── sources/
│   │   ├── init.lua
│   │   ├── local_dir.lua
│   │   ├── m3u.lua
│   │   ├── rss.lua
│   │   └── url.lua
│   │
│   ├── players/
│   │   ├── init.lua
│   │   ├── base.lua
│   │   ├── mpv.lua
│   │   └── system.lua
│   │
│   ├── tui/
│   │   ├── init.lua
│   │   ├── app.lua
│   │   ├── screen.lua
│   │   ├── list.lua
│   │   ├── statusbar.lua
│   │   ├── helpbar.lua
│   │   ├── input.lua
│   │   └── theme.lua
│   │
│   └── util/
│       ├── fs.lua
│       ├── json.lua
│       ├── log.lua
│       └── time.lua
│
├── examples/
│   ├── play_local.lua
│   └── tui_demo.lua
│
├── spec/
│   ├── library_spec.lua
│   ├── playlist_spec.lua
│   └── sources_spec.lua
│
└── rockspecs/
    └── musiclua-scm-1.rockspec
```

---

## 9. 模块职责

### 9.1 `bin/musiclua`

命令行入口。

职责：

1. 解析命令行参数。
2. 加载配置。
3. 启动 `musiclua.app`。
4. 支持常见命令：

```bash
musiclua
musiclua ~/Music
musiclua tui ~/Music
musiclua play ~/Music
musiclua version
```

第一阶段只需要支持：

```bash
musiclua [directory]
```

如果没有提供目录，默认使用：

```text
~/Music
```

---

### 9.2 `musiclua/init.lua`

包入口。

职责：

1. 暴露版本号。
2. 暴露 `new()`。
3. 暴露 `run_tui()`。
4. 方便其他 Lua 项目以库方式调用。

建议 API：

```lua
local musiclua = require("musiclua")

musiclua.run_tui({
    library = "~/Music",
    player = "mpv",
})
```

---

### 9.3 `musiclua/version.lua`

版本信息。

内容：

```lua
major = 0
minor = 1
patch = 0
string = "0.1.0"
```

---

### 9.4 `musiclua/app.lua`

应用编排层。

职责：

1. 初始化配置。
2. 初始化 library。
3. 初始化 playlist。
4. 初始化 player。
5. 初始化 TUI。
6. 处理应用生命周期。
7. 退出时清理资源。

核心流程：

```text
解析参数
加载配置
扫描音乐库
创建播放列表
启动播放器
启动 TUI
进入主循环
退出
关闭播放器
```

---

### 9.5 `musiclua/config.lua`

配置管理。

第一版配置项：

```lua
{
    library_dirs = {
        "~/Music"
    },

    audio_extensions = {
        ".mp3",
        ".ogg",
        ".wav",
        ".flac",
        ".m4a",
        ".opus"
    },

    player = "mpv",

    tui = {
        theme = "default",
        show_helpbar = true,
        show_statusbar = true,
    },

    mpv = {
        ipc_path = nil,
        volume = 80,
        autoplay = false,
    }
}
```

配置优先级：

```text
命令行参数 > 配置文件 > 默认值
```

第一版可以只做默认值和命令行参数，不强制读取配置文件。

---

### 9.6 `musiclua/library.lua`

音乐库。

职责：

1. 扫描一个或多个目录。
2. 调用 sources。
3. 返回 track 列表。
4. 排序。
5. 去重。

第一阶段 API：

```lua
library.scan_dir(path)
library.scan_dirs(paths)
```

返回：

```lua
{
    {
        id = "1",
        title = "demo.mp3",
        path = "/home/user/Music/demo.mp3",
        source = "local",
    },
    ...
}
```

---

### 9.7 `musiclua/track.lua`

Track 数据模型。

字段：

```lua
{
    id = string,
    title = string,
    path = string,
    source = "local" | "m3u" | "rss" | "url",
    duration = number | nil,
    artist = string | nil,
    album = string | nil,
    added_at = number | nil,
}
```

第一阶段只需要：

```lua
id
title
path
source
```

---

### 9.8 `musiclua/playlist.lua`

播放列表。

职责：

1. 保存 tracks。
2. 当前播放索引。
3. 上一首。
4. 下一首。
5. 随机播放。
6. 循环模式。

第一阶段 API：

```lua
playlist.new(tracks)
playlist:add(track)
playlist:current()
playlist:next()
playlist:prev()
playlist:goto_index(index)
playlist:count()
playlist:set_tracks(tracks)
playlist:filter(keyword)
playlist:clear_filter()
```

循环模式后续支持：

```lua
none
one
all
```

第一阶段可以先只做：

```text
none / all
```

---

### 9.9 `musiclua/sources/init.lua`

Source 注册中心。

职责：

1. 管理不同 source。
2. 根据输入类型选择 source。

例如：

```text
本地目录 -> local_dir
.m3u 文件 -> m3u
rss url -> rss
http audio url -> url
```

---

### 9.10 `musiclua/sources/local_dir.lua`

本地目录 source。

第一阶段核心 source。

职责：

1. 扫描目录。
2. 判断音频扩展名。
3. 生成 track。
4. 可选递归扫描。

第一阶段建议：

```text
只扫描一级目录。
后续再加递归扫描。
```

支持扩展名：

```text
.mp3
.ogg
.wav
.flac
.m4a
.opus
```

---

### 9.11 `musiclua/sources/m3u.lua`

M3U 播放列表 source。

后续阶段实现。

职责：

1. 解析 `.m3u` / `.m3u8`。
2. 支持相对路径。
3. 支持绝对路径。
4. 忽略注释行 `#`。
5. 可选解析 `#EXTINF`。

---

### 9.12 `musiclua/sources/rss.lua`

RSS/Podcast source。

后续阶段实现。

职责：

1. 请求 RSS XML。
2. 解析 `<enclosure url="...">`。
3. 生成 track。
4. 只处理合法 feed。

第一阶段不实现。

---

### 9.13 `musiclua/sources/url.lua`

直接 URL source。

后续阶段实现。

职责：

1. 接收用户提供的合法音频 URL。
2. 检查协议是否为 `http/https`。
3. 生成 track。
4. 交给 mpv 播放。

第一阶段不实现。

---

### 9.14 `musiclua/players/init.lua`

Player 工厂。

职责：

根据配置创建播放器：

```lua
players.create("mpv")
players.create("system")
```

第一阶段只需要支持：

```lua
mpv
```

---

### 9.15 `musiclua/players/base.lua`

播放器接口。

必须定义以下方法：

```lua
player:new(options)
player:load(track)
player:play()
player:pause()
player:toggle()
player:stop()
player:next()
player:prev()
player:set_volume(value)
player:get_volume()
player:get_status()
player:get_position()
player:get_duration()
player:close()
```

---

### 9.16 `musiclua/players/mpv.lua`

mpv 播放器实现。

职责：

1. 启动 mpv idle 实例。
2. 创建或连接 IPC socket。
3. 发送 JSON command。
4. 接收 JSON response。
5. 查询播放状态。
6. 处理 track 切换。
7. 退出时关闭 mpv。

mpv 启动参数建议：

```bash
mpv --no-video --idle=yes --input-ipc-server=<ipc_path>
```

Linux/macOS：

```text
/tmp/musiclua-<pid>.sock
```

JSON IPC 命令示例：

加载文件：

```json
{"command":["loadfile","/path/to/audio.mp3"]}
```

暂停：

```json
{"command":["set_property","pause",true]}
```

继续：

```json
{"command":["set_property","pause",false]}
```

切换暂停：

```json
{"command":["cycle","pause"]}
```

下一首：

```json
{"command":["playlist-next"]}
```

上一首：

```json
{"command":["playlist-prev"]}
```

设置音量：

```json
{"command":["set_property","volume",80]}
```

获取播放进度：

```json
{"command":["get_property","time-pos"]}
```

获取时长：

```json
{"command":["get_property","duration"]}
```

退出 mpv：

```json
{"command":["quit"]}
```

---

### 9.17 `musiclua/players/system.lua`

备用播放器。

后续可选。

职责：

```text
调用系统命令播放音频。
```

不推荐第一阶段使用。

---

### 9.18 `musiclua/tui/init.lua`

TUI 入口。

API：

```lua
require("musiclua.tui").run(options)
```

---

### 9.19 `musiclua/tui/app.lua`

TUI 主应用。

职责：

1. 初始化 screen。
2. 渲染 list。
3. 渲染 statusbar。
4. 渲染 helpbar。
5. 监听输入。
6. 定时刷新播放状态。
7. 处理退出。

主循环逻辑：

```text
while running do
    draw()
    key = input.read_nonblocking()
    handle_key(key)
    update_player_status()
    sleep(frame_interval)
end
```

建议刷新间隔：

```text
0.05s 到 0.1s
```

---

### 9.20 `musiclua/tui/screen.lua`

屏幕抽象。

职责：

1. 初始化 curses。
2. 清屏。
3. 获取终端宽高。
4. 绘制文本。
5. 设置颜色。
6. 关闭 curses。

---

### 9.21 `musiclua/tui/list.lua`

列表组件。

职责：

1. 显示 tracks。
2. 高亮当前选中项。
3. 上下滚动。
4. 支持过滤。
5. 显示序号。
6. 显示当前播放项标记。

显示格式：

```text
  1  song_a.ogg
  2  song_b.mp3
> 3  song_c.flac
  4  podcast_episode_01.mp3
```

如果当前正在播放，可以显示：

```text
▶
```

例如：

```text
> ▶ 3 song_c.flac
```

---

### 9.22 `musiclua/tui/statusbar.lua`

状态栏组件。

显示：

```text
Now Playing: song_c.flac
Status: playing | 01:24 / 03:52 | volume: 80%
```

字段：

```text
当前歌曲
播放状态
当前位置
总时长
音量
```

播放状态：

```text
playing
paused
stopped
loading
```

---

### 9.23 `musiclua/tui/helpbar.lua`

快捷键帮助栏。

显示：

```text
[enter] play  [space] pause  [n] next  [p] prev  [/] search  [q] quit
```

---

### 9.24 `musiclua/tui/input.lua`

输入处理。

职责：

1. 非阻塞读取按键。
2. 映射标准按键。
3. 处理搜索输入模式。

第一阶段按键：

```text
q       退出
j       下移
k       上移
Down    下移
Up      上移
Enter   播放当前项
space   暂停/继续
n       下一首
p       上一首
+       音量增加
-       音量减少
/       进入搜索
Esc     取消搜索/清除过滤
```

---

### 9.25 `musiclua/tui/theme.lua`

主题。

职责：

1. 颜色配置。
2. 图标配置。
3. 边框样式。

默认主题：

```lua
{
    playing_icon = "▶",
    selected_icon = ">",
    stopped_icon = " ",
    paused_text = "paused",
    playing_text = "playing",
}
```

---

### 9.26 `musiclua/util/fs.lua`

文件系统工具。

需要提供：

```lua
fs.exists(path)
fs.is_file(path)
fs.is_dir(path)
fs.expand_user(path)
fs.has_audio_ext(path, extensions)
fs.join_path(a, b)
```

---

### 9.27 `musiclua/util/json.lua`

JSON 工具。

建议封装 `dkjson`。

API：

```lua
json.encode(table)
json.decode(string)
```

如果 `dkjson` 不可用，可提示用户安装。

---

### 9.28 `musiclua/util/log.lua`

日志工具。

级别：

```text
debug
info
warn
error
```

第一阶段可以只写到 stderr，不写文件。

---

### 9.29 `musiclua/util/time.lua`

时间工具。

API：

```lua
time.format_seconds(84)
```

返回：

```text
01:24
```

如果 duration 为 nil：

```text
00:00
```

---

## 10. 核心数据模型

### 10.1 Track

```lua
Track = {
    id = string,
    title = string,
    path = string,
    source = string,
    duration = number | nil,
    artist = string | nil,
    album = string | nil,
}
```

---

### 10.2 PlaylistState

```lua
PlaylistState = {
    tracks = Track[],
    filtered_tracks = Track[],
    selected_index = number,
    playing_index = number,
    filter_keyword = string | nil,
}
```

---

### 10.3 PlayerState

```lua
PlayerState = {
    status = "playing" | "paused" | "stopped" | "loading",
    position = number,
    duration = number,
    volume = number,
    current_track = Track | nil,
}
```

---

### 10.4 TUIState

```lua
TUIState = {
    mode = "normal" | "search",
    search_input = string,
    message = string | nil,
    running = boolean,
}
```

---

## 11. MVP 功能细节

## 11.1 命令行

第一版支持：

```bash
musiclua
musiclua ~/Music
musiclua ./music
```

行为：

1. 如果参数为空，默认扫描 `~/Music`。
2. 如果目录不存在，报错并退出。
3. 如果目录没有音频文件，进入 TUI 并显示空状态。

空状态显示：

```text
No audio files found.
```

---

## 11.2 文件扫描

扫描规则：

1. 默认扫描一级目录。
2. 忽略隐藏文件和隐藏目录。
3. 只识别配置的音频扩展名。
4. 按文件名排序。
5. 返回 track 列表。

---

## 11.3 TUI 模式

默认模式：

```text
normal
```

搜索模式：

```text
search
```

normal 模式下按键会执行操作。

search 模式下按键输入搜索内容。

search 模式下：

```text
Enter 确认过滤
Esc 取消搜索
Backspace 删除字符
普通字符追加到搜索关键字
```

---

## 11.4 列表过滤

过滤规则：

1. 不区分大小写。
2. 匹配 title。
3. 如果关键字为空，显示全部。
4. 过滤后 selected_index 重置为 1。
5. 清除过滤后恢复原列表和合理选中位置。

---

## 11.5 播放逻辑

当用户按 `Enter`：

```text
取当前选中 track
player:load(track)
player:play()
playlist.playing_index = selected_index
statusbar 更新
```

当用户按 `n`：

```text
playlist:next()
player:load(current)
player:play()
```

当用户按 `p`：

```text
playlist:prev()
player:load(current)
player:play()
```

当用户按 `space`：

```text
player:toggle()
```

---

## 11.6 退出逻辑

按 `q`：

```text
player:close()
screen:cleanup()
os.exit(0)
```

必须保证：

1. mpv 进程退出。
2. socket 文件清理。
3. 终端恢复正常。
4. curses 状态释放。

---

## 12. 开发里程碑

---

# Milestone 1：基础包骨架

目标：

```text
项目可以 require，可以执行 bin/musiclua。
```

涉及文件：

```text
bin/musiclua
musiclua/init.lua
musiclua/version.lua
musiclua/app.lua
```

验收标准：

```bash
./bin/musiclua version
```

输出：

```text
musiclua 0.1.0
```

---

# Milestone 2：本地音乐扫描

目标：

```text
能扫描目录中的音频文件。
```

涉及文件：

```text
musiclua/library.lua
musiclua/track.lua
musiclua/sources/init.lua
musiclua/sources/local_dir.lua
musiclua/util/fs.lua
```

验收标准：

给定目录：

```text
test_music/
├── a.mp3
├── b.ogg
├── c.txt
└── d.flac
```

调用：

```lua
library.scan_dir("test_music")
```

返回：

```text
a.mp3
b.ogg
d.flac
```

不返回：

```text
c.txt
```

---

# Milestone 3：播放列表

目标：

```text
能管理 track 列表。
```

涉及文件：

```text
musiclua/playlist.lua
```

验收标准：

支持：

```lua
playlist:set_tracks(tracks)
playlist:current()
playlist:next()
playlist:prev()
playlist:goto_index(3)
playlist:filter("live")
playlist:clear_filter()
```

并且边界情况安全：

1. 空列表不崩溃。
2. 到最后一首 next 后回到第一首。
3. 到第一首 prev 后回到最后一首。
4. 过滤后索引正确。

---

# Milestone 4：mpv 播放后端

目标：

```text
能通过 mpv 播放本地音频。
```

涉及文件：

```text
musiclua/players/init.lua
musiclua/players/base.lua
musiclua/players/mpv.lua
musiclua/util/json.lua
musiclua/util/log.lua
musiclua/util/time.lua
```

验收标准：

可以通过代码执行：

```lua
local players = require("musiclua.players")
local player = players.create("mpv")

player:load({
    path = "/path/to/demo.mp3",
    title = "demo.mp3",
})

player:play()
player:pause()
player:toggle()
player:set_volume(70)
player:close()
```

要求：

1. mpv 能启动。
2. 能播放。
3. 能暂停。
4. 能退出。
5. 不残留 mpv 进程。

---

# Milestone 5：TUI 列表

目标：

```text
终端显示音乐列表。
```

涉及文件：

```text
musiclua/tui/init.lua
musiclua/tui/app.lua
musiclua/tui/screen.lua
musiclua/tui/list.lua
musiclua/tui/input.lua
musiclua/tui/theme.lua
```

验收标准：

运行：

```bash
musiclua ./test_music
```

显示列表。

支持：

```text
j/k 移动
q 退出
```

此时可以不播放。

---

# Milestone 6：TUI 播放控制

目标：

```text
TUI 能真正控制 mpv。
```

涉及文件：

```text
musiclua/tui/app.lua
musiclua/players/mpv.lua
musiclua/playlist.lua
```

验收标准：

支持：

```text
Enter 播放
space 暂停/继续
n 下一首
p 上一首
q 退出
```

---

# Milestone 7：状态栏和搜索

目标：

```text
完整 MVP。
```

涉及文件：

```text
musiclua/tui/statusbar.lua
musiclua/tui/helpbar.lua
musiclua/tui/input.lua
musiclua/util/time.lua
```

验收标准：

1. 状态栏显示当前歌曲。
2. 状态栏显示播放状态。
3. 状态栏显示时间。
4. 状态栏显示音量。
5. `/` 可以搜索。
6. `Esc` 可以清除搜索。

---

# Milestone 8：配置系统

目标：

```text
支持配置文件。
```

涉及文件：

```text
musiclua/config.lua
config.example.lua
```

配置文件位置建议：

```text
~/.config/musiclua/config.lua
```

第一版可以只支持 Lua table 配置。

---

# Milestone 9：M3U 播放列表

目标：

```text
支持导入 .m3u/.m3u8。
```

涉及文件：

```text
musiclua/sources/m3u.lua
```

验收标准：

给定：

```text
playlist.m3u
```

能解析为 tracks。

---

# Milestone 10：合法 RSS/Podcast

目标：

```text
支持合法 RSS/Podcast。
```

涉及文件：

```text
musiclua/sources/rss.lua
musiclua/util/http.lua 可选
```

要求：

1. 只请求用户提供的 RSS URL。
2. 不抓取版权网站。
3. 不绕过任何访问限制。
4. 只解析 enclosure 音频地址。

---

## 13. 验收标准

### 13.1 MVP 验收标准

完成以下全部功能，即可认为 v0.1.0 完成：

1. `musiclua ~/Music` 可以启动。
2. 能扫描本地音频文件。
3. TUI 能显示列表。
4. `j/k` 能移动。
5. `Enter` 能播放。
6. `space` 能暂停/继续。
7. `n/p` 能切歌。
8. `/` 能搜索。
9. `q` 能退出。
10. 退出后 mpv 不残留。
11. 空目录不崩溃。
12. 无音频目录不崩溃。
13. 没有安装 mpv 时给出清晰错误。

---

### 13.2 代码质量要求

1. 所有模块使用局部模块表返回。
2. 不使用全局变量污染。
3. 不直接 print 调试信息到 TUI 中央区域。
4. 错误通过 log 或 TUI message 显示。
5. 每个 util 函数尽量纯函数化。
6. playlist 逻辑必须可单测。
7. library 逻辑必须可单测。
8. source 必须可扩展。
9. player 必须面向接口编程。
10. TUI 渲染和输入处理分离。

---

## 14. 测试计划

### 14.1 单元测试

测试框架建议：

```text
busted
```

需要测试：

```text
spec/library_spec.lua
spec/playlist_spec.lua
spec/sources_spec.lua
```

---

### 14.2 library 测试

测试点：

1. 空目录返回空列表。
2. 非音频文件不返回。
3. 大小写扩展名可识别。
4. 文件按标题排序。
5. 隐藏文件忽略。

---

### 14.3 playlist 测试

测试点：

1. 空 playlist current 返回 nil。
2. next 到末尾回到开头。
3. prev 到开头回到末尾。
4. goto_index 越界安全。
5. filter 后列表正确。
6. clear_filter 恢复列表。

---

### 14.4 source 测试

测试点：

1. local_dir 只接受目录。
2. local_dir 返回合法 track。
3. m3u source 后续测试解析规则。
4. rss source 后续只测试 XML 解析，不测试非法抓取。

---

### 14.5 手动测试

手动测试脚本：

```bash
mkdir -p ./test_music
touch ./test_music/a.mp3
touch ./test_music/b.ogg
touch ./test_music/c.txt
touch ./test_music/d.flac
```

运行：

```bash
./bin/musiclua ./test_music
```

检查 TUI 是否只显示：

```text
a.mp3
b.ogg
d.flac
```

---

## 15. 风险与备选方案

### 15.1 curses 安装困难

风险：

```text
lua-curses 在某些系统难安装。
```

备选：

```text
改用 ANSI escape codes。
```

ANSI 简版功能：

1. 清屏。
2. 光标移动。
3. 简单高亮。
4. 状态栏。
5. 键盘输入。

---

### 15.2 Windows 兼容问题

风险：

```text
Unix socket、curses、stty 在 Windows 行为不同。
```

策略：

1. 第一阶段优先 Linux/macOS。
2. Windows 后续支持。
3. Windows 可先使用 WSL。
4. mpv IPC 在 Windows 需要使用 named pipe，后续再处理。

---

### 15.3 mpv 不存在

风险：

```text
用户没有安装 mpv。
```

处理：

启动时检测：

```bash
mpv --version
```

如果不存在，显示：

```text
Error: mpv is required but not installed.
```

并退出。

---

### 15.4 mpv IPC 异步复杂

风险：

```text
JSON IPC 需要异步读取状态。
```

策略：

1. 第一版可以轮询状态。
2. 每 200ms 查询一次 time-pos/duration。
3. 不追求实时波形或精确进度。
4. 后续再改成事件监听。

---

### 15.5 TUI 闪烁

风险：

```text
频繁重绘导致闪烁。
```

策略：

1. 只重绘变化区域。
2. 使用 curses 的 refresh 机制。
3. 降低状态栏刷新频率。
4. 列表只在选中项变化时重绘。

---

## 16. 版本规划

### v0.1.0

```text
本地音乐 TUI 播放器。
```

功能：

1. 本地扫描。
2. TUI 列表。
3. mpv 播放。
4. 基本控制。
5. 搜索。

---

### v0.2.0

```text
配置系统和 M3U。
```

功能：

1. config.lua。
2. M3U 播放列表。
3. 递归扫描。
4. 排序方式。
5. 播放模式。

---

### v0.3.0

```text
合法网络源。
```

功能：

1. RSS/Podcast。
2. 合法 URL 播放。
3. 简单缓存。
4. 下载合法音频到本地缓存。

---

### v0.4.0

```text
增强 TUI。
```

功能：

1. 主题。
2. 多栏布局。
3. 文件信息面板。
4. 帮助页面。
5. 日志面板。

---

## 17. 给 Trae 的开发指令

你可以把下面这段直接交给 Trae。

---

### Trae 任务总提示词

```text
你现在是实现 musiclua 项目的开发助手。

请严格按照下面的项目计划书实现，不要随意扩大范围。

项目目标：
实现一个 Lua TUI 音乐播放器包，第一阶段只做本地音乐播放，不做 GUI，不做版权内容抓取。

技术栈：
- Lua 5.4
- luafilesystem
- luasocket
- dkjson
- lua-curses
- mpv 作为播放后端

当前目录结构已经创建完成，请基于现有文件写入代码。

开发优先级：
1. bin/musiclua 和 musiclua/init.lua
2. musiclua/version.lua
3. musiclua/util/fs.lua
4. musiclua/library.lua
5. musiclua/sources/local_dir.lua
6. musiclua/playlist.lua
7. musiclua/players/base.lua
8. musiclua/players/mpv.lua
9. musiclua/tui/list.lua
10. musiclua/tui/app.lua
11. musiclua/tui/statusbar.lua

要求：
- 不要引入新的顶层目录。
- 不要使用全局变量。
- 每个模块都 return local table。
- 优先保证 MVP 可运行。
- 如果某个依赖不可用，给出清晰错误，不要静默失败。
- TUI 使用 curses。
- 如果 curses 不可用，提供清晰的安装提示。
- 播放器只实现 mpv。
- 第一阶段不实现 rss.lua、url.lua、system.lua。
- 所有网络源只允许合法来源，不实现版权抓取。

第一版验收标准：
运行 ./bin/musiclua ./test_music 后，可以显示本地音频列表。
支持 j/k 移动，Enter 播放，space 暂停，n/p 切歌，/ 搜索，q 退出。
退出后 mpv 不残留。
```

---

### Trae 分阶段提示词

#### 第一阶段：基础骨架

```text
请先实现 musiclua 的基础骨架。

需要写入以下文件：
- bin/musiclua
- musiclua/init.lua
- musiclua/version.lua
- musiclua/app.lua

要求：
1. bin/musiclua 是可执行 Lua 脚本。
2. 支持 ./bin/musiclua version 输出版本。
3. 支持 ./bin/musiclua [directory] 启动应用。
4. 如果没有 directory，默认使用 ~/Music。
5. musiclua/init.lua 暴露 _VERSION 和 run_tui。
6. 暂时不实现 TUI，可以先打印即将启动的信息。
```

---

#### 第二阶段：本地扫描

```text
请实现本地音乐扫描。

需要写入以下文件：
- musiclua/util/fs.lua
- musiclua/track.lua
- musiclua/library.lua
- musiclua/sources/init.lua
- musiclua/sources/local_dir.lua

要求：
1. 支持扫描目录一级文件。
2. 支持扩展名 .mp3 .ogg .wav .flac .m4a .opus。
3. 忽略隐藏文件。
4. 按文件名排序。
5. 返回 track table。
6. 每个 track 包含 id、title、path、source。
7. source 固定为 "local"。
8. 不要递归扫描，除非配置中明确要求。
```

---

#### 第三阶段：播放列表

```text
请实现播放列表。

需要写入：
- musiclua/playlist.lua

要求：
1. 支持 set_tracks。
2. 支持 current。
3. 支持 next。
4. 支持 prev。
5. 支持 goto_index。
6. 支持 filter。
7. 支持 clear_filter。
8. 空列表时所有操作安全。
9. next 到末尾后回到开头。
10. prev 到开头后回到末尾。
11. filter 不区分大小写。
```

---

#### 第四阶段：mpv 播放器

```text
请实现 mpv 播放器后端。

需要写入：
- musiclua/players/init.lua
- musiclua/players/base.lua
- musiclua/players/mpv.lua
- musiclua/util/json.lua
- musiclua/util/log.lua
- musiclua/util/time.lua

要求：
1. 使用 mpv --no-video --idle=yes --input-ipc-server。
2. socket 路径默认使用 /tmp/musiclua-<pid>.sock。
3. 支持 load、play、pause、toggle、next、prev、set_volume、close。
4. 使用 JSON IPC。
5. 如果 mpv 不存在，报清晰错误。
6. 如果 socket 连接失败，报清晰错误。
7. close 时发送 quit，并清理资源。
8. 不要阻塞 TUI 主循环。
9. 第一版可以轮询状态。
```

---

#### 第五阶段：TUI

```text
请实现 TUI。

需要写入：
- musiclua/tui/init.lua
- musiclua/tui/app.lua
- musiclua/tui/screen.lua
- musiclua/tui/list.lua
- musiclua/tui/statusbar.lua
- musiclua/tui/helpbar.lua
- musiclua/tui/input.lua
- musiclua/tui/theme.lua

要求：
1. 使用 lua-curses。
2. 显示标题栏。
3. 显示歌曲列表。
4. 显示状态栏。
5. 显示帮助栏。
6. 支持 j/k/Up/Down 移动。
7. 支持 Enter 播放。
8. 支持 space 暂停/继续。
9. 支持 n/p 切歌。
10. 支持 / 搜索。
11. 支持 Esc 清除搜索。
12. 支持 q 退出。
13. 退出时恢复终端。
14. 空列表显示 No audio files found.
15. TUI 不应阻塞 mpv 状态更新。
```

---

## 18. 最终完成标准

当下面命令可以稳定运行：

```bash
./bin/musiclua ~/Music
```

并且具备：

```text
本地扫描
TUI 列表
Enter 播放
space 暂停
n/p 切歌
/ 搜索
q 退出
状态栏显示
mpv 正常关闭
```

即可认为 `musiclua v0.1.0 MVP` 完成。

---

## 19. 后续扩展方向

MVP 完成后，可以按顺序做：

1. 配置文件。
2. 递归扫描。
3. M3U 播放列表。
4. 播放模式：顺序、随机、单曲循环。
5. 合法 RSS/Podcast。
6. 合法 URL 播放。
7. 下载缓存。
8. 主题系统。
9. 更多信息面板。
10. luarocks 发布。

---

## 20. 项目一句话总结

```text
musiclua 是一个 Lua 编写的 TUI 音乐播放器包。第一阶段专注本地音乐扫描与 mpv 播放，后续可扩展到合法 RSS/Podcast 和音频 URL。项目采用模块化架构，分别由 library、playlist、player、tui 四层组成。
```